# Online setup

This build is already configured with the supplied Supabase project URL and publishable key.

Supabase URL:
https://jxlfaxspqwrypuqyicgo.supabase.co

If using a new Supabase project, run `supabase.sql` in the Supabase SQL Editor and update the Supabase values inside `index.html`.

Do not use or publish the secret key.
